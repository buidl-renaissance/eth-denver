# Database Configuration
# For local development, set USE_LOCAL=true to use SQLite file
USE_LOCAL=true

# For production with Turso
TURSO_DATABASE_URL=libsql://your-database.turso.io
TURSO_AUTH_TOKEN=your-auth-token

# ETHDenver events import (POST /api/events/import)
# Option A: Publish the sheet to web (File → Share → Publish to web → CSV) and set the URL:
# ETHDENVER_SHEETS_CSV_URL=https://docs.google.com/spreadsheets/d/e/.../pub?output=csv
# Option B: Use Google Sheets API (sheet must be "Anyone with the link can view"):
# GOOGLE_SHEETS_API_KEY=your-google-api-key
